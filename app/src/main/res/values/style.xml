<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="customButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#00000000</item> <!-- Transparent tint -->
    </style>

    <style name="CustomDatePicker" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- Customize primary colors -->
        <item name="colorAccent">@color/dark_green</item>
    </style>

    <style name="spinner_style">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:showDividers">none</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:popupBackground">@color/white</item>
        <item name="android:dropDownHeight">wrap_content</item>
        <item name="android:layout_width">40dp</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:dropDownSelector">@color/green</item>
        <item name="android:dropDownWidth">match_parent</item>
    </style>


    <style name="SpinnerPromptTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/gray</item> <!-- Text color -->
        <item name="android:textSize">13sp</item> <!-- Text size -->
    </style>

    <style name="CustomCalendarTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/dark_green</item>  <!-- Selected day background -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:colorControlActivated">@color/dark_green</item>
    </style>

    <!-- Calendar Day Text -->
    <style name="CustomDateText">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Calendar Week Day Text -->
    <style name="CustomWeekDayText">
        <item name="android:textColor">@color/dark_green</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Custom text style for the month -->
    <style name="CustomMonthText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">10sp</item> <!-- Set desired text size -->
        <item name="android:textColor">@color/black</item> <!-- Set text color -->
        <item name="android:fontFamily">@font/roboto_regular</item> <!-- Optional: Custom font -->
    </style>

</resources>